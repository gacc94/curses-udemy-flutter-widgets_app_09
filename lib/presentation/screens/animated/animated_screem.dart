import 'package:flutter/material.dart';

class AnimatedScreem extends StatelessWidget {
  static const String name = 'AnimatedScreen';
  const AnimatedScreem({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Animated Screen')));
  }
}
